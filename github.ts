// GitHub operations now run only in /api/cms on Vercel. This client module is
// deliberately limited to UI-safe constants; no token or GitHub API is present.
export const MAX_FILE_BYTES = 25 * 1024 * 1024;
